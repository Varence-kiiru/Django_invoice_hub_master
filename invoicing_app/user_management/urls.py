"""
URL patterns for user management - currently empty.
API endpoints are registered in core/urls.py under /api/system/
"""
from django.urls import path

app_name = 'user_management'

urlpatterns = []
